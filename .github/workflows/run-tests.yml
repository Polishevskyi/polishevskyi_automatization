name: Gradle Tests with Allure

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Клонування репозиторію
      - name: Checkout repository
        uses: actions/checkout@v3

      # Встановлення JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # Кешування залежностей Gradle
      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # Завантаження залежностей і компіляція
      - name: Build and resolve dependencies
        run: ./gradlew build -x test --no-daemon

      # Запуск тестів
      - name: Run tests
        run: ./gradlew test --no-daemon

      # Генерація Allure звіту
      - name: Generate Allure Report
        run: ./gradlew allureReport --no-daemon

      # Завантаження Allure результатів як артефакт
      - name: Upload Allure Results
        uses: actions/upload-artifact@v3
        with:
          name: allure-results
          path: build/allure-results

      # Завантаження Allure звіту як артефакт
      - name: Upload Allure Report
        uses: actions/upload-artifact@v3
        with:
          name: allure-report
          path: build/reports/allure-report
